v5版本使用了异步拉起同步的方式，通过epoll监听管道的读端的方式，在给父进程发送SIGUSR1信号时（异步事件），
执行信号处理函数的过程中，往管道随便写点数据，目的是拉起同步事件，将epoll_wait激活，此时激活的来源是
监听的管道中有数据到来，那么此时就可以执行这个管道到来时的处理逻辑
在这个处理逻辑中来进行子进程的关闭操作。
那么此时也会有问题，如果此时数据在传送了，客户端正在接收数据，给服务端发送SIGUSR1信号怎么办？
---> 测试结果是，会出错（原因是老师代码没写好），所以也不完善

所以看v6版本