v6版本是通过父子进程间建立的管道sockpair，父进程给子进程通过管道发送一个文本数据，即exit标志，如果给他发送的标志是1，
表示要让子进程退出。
那么如果在发送这样一个退出标志的时候，工作进程正在处理任务，通过代码逻辑，此时工作进程是无法接收这个任务的，
所以此时正在工作的子进程是无法收到这个退出标志，即只有在任务完成时才能接收到这个退出标志，然后工作进程才会退出，
这样就保证了，在任务完成之前，服务端不会断开。